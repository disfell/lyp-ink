# registry=https://registry.npmmirror.com/
registry=https://registry.npm.taobao.org/
# 关闭了npm对SSL证书的严格检查
strict-ssl=false
# 默认值^，意味着npm会保存依赖的“合理”版本范围，而不是精确版本
save-prefix=
# 安装的确切版本号，而不是使用版本范围
save-exact=true
# 这行配置确保npm在安装包时显示进度条，让你可以看到安装的进度
progress=true